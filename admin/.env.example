# Copy to admin/.env and fill in. Never commit the real file.
# The service_role key must NOT go here - anything needing it belongs in a
# Supabase edge function, because anything in a React bundle is public.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
